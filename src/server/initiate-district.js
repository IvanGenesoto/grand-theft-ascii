module.exports = function initiateDistrict() {

  const district = this
  const {players, characters, vehicles, rooms, initiate, io, now, handleSocket} = district // eslint-disable-line no-unused-vars
}
